apiVersion: v1
kind: Service
metadata:
  name: {{ include "norcedrillinggravitationalfieldservice.fullname" . }}
  labels:
    {{- include "norcedrillinggravitationalfieldservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "norcedrillinggravitationalfieldservice.selectorLabels" . | nindent 4 }}
